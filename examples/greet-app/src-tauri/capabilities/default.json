{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lets the webview call the ConnectRPC services this app hosts.",
  "windows": ["main"],
  "permissions": ["core:default", "connectrpc-tauri:default"]
}
